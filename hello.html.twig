<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
     rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous">
      <link rel="stylesheet" href="C:\xampp\htdocs\Twig\views\style.scss">
    <title> {{name}}</title>
</head>
<body>
    
        <div class="row">
            <div class="col-sm-12">
                <table class="table table-bordered">
                    <caption>
                        <strong>Student grade manegaement system</strong> - {{type}} <em>{{owner}}</em>
                    </caption>
                    <thead class="bg-light">
                        <tr>
                            <th>Name</th>
                            <th>E-mail</th>
                            <th>Status</th>
                            <th>Grade</th>
                        
                        </tr>
                    </thead>
                    <tbody>
                        {%for member in studen_list %}
                        <tr class=" 
                        {% if member.status == '0' %} text-info {% endif %}
                        {% if member.status == 'F' %} bg-danger {% endif %} 
                        {% if member.status == 'E' %} bg-warning {% endif %} 

                        ">
                     
                        <td> <strong>{{member.name}}</strong></td>
                        <td>{{member.email}}</td>
                        <td>{{member.status}}</td>
                       <td>
                           {% set id = member.id %}
         <form action="./process.php?" method="POST">
                       <div class="row">
                       <div class="col-6">
                      
                       <select class="form-select form-select-sm" name="select" aria-label=".form-select-sm example"  >
                            <option selected>{{member.grade}}
                            </option>
                            <option value="A">A</option>
                            <option value="B">B</option>
                            <option value="C">C</option>
                            <option value="D">D</option>
                            <option value="E">E</option>
                            <option value="F">F</option>
                            <option value="NULL">NULL</option>
                              </select>
                        </div>
                    
                        <div class="col-3">
                        
                        <button class="btn btn-light btn-sm">
                        <a href="process.php?saveid={{id}}"/>
                        save</button>
    
                       
                       <!--  <a href="./process.php?saveid={{id}}" class="btn btn-light btn-sm" name="saveid">save</a>-->
                            <!--<button class="btn btn-light btn-sm">
                             <a href="./process.php?/saveid={{id}}"
                            class="text-dark" >save</button>-->
                        <!--    <a href="./process.php?saveid={{id}}" class="btn btn-light btn-sm" name="saveid">save</a>-->
                        </div>
                        </td>
                    </div>
                        </div>
                       
                </form>
   </tr>
     {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>